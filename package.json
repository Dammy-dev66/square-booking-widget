{
  "name": "square-booking-widget",
  "version": "1.0.0",
  "description": "Custom Square booking widget with checkout flow",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "square": "^38.0.0"
  },
  
  "start": "node server.js",
  "build": "echo 'No build step'",
  "test": "echo \"Error: no test specified\" && exit 1"

  "engines": {
    "node": "22.x"
  }
}
